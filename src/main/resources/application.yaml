spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/code_review_db
    username: postgres
    password:

  flyway:
    enabled: true
    baseline-on-migrate: true
    url: jdbc:postgresql://localhost:5432/code_review_db
    user: postgres
    password:

  mail:
    port: 587
    host: smtp.gmail.com
    username: Ahmedgarci146@gmail.com
    password: ozlhhngxhiyziqzm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8089

